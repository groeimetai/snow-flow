---
title: CLI
description: Snow-Flow CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Snow-Flow CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
snow-code
```

But it also accepts commands as documented on this page. This allows you to interact with Snow-Flow programmatically.

```bash
snow-code run "Explain how closures work in JavaScript"
```

---

### tui

Start the Snow-Flow terminal user interface.

```bash
snow-code [project]
```

#### Flags

| Flag         | Short | Description                                |
| ------------ | ----- | ------------------------------------------ |
| `--continue` | `-c`  | Continue the last session                  |
| `--session`  | `-s`  | Session ID to continue                     |
| `--prompt`   |       | Prompt to use                              |
| `--model`    | `-m`  | Model to use in the form of provider/model |
| `--agent`    |       | Agent to use                               |
| `--port`     |       | Port to listen on                          |
| `--hostname` |       | Hostname to listen on                      |

---

## Commands

The Snow-Flow CLI also has the following commands.

---

### agent

Manage agents for Snow-Flow.

```bash
snow-code agent [command]
```

---

### attach

Attach a terminal to an already running Snow-Flow backend server started via `serve` or `web` commands.

```bash
snow-code attach [url]
```

This allows using the TUI with a remote Snow-Flow backend. For example:

```bash
# Start the backend server for web/mobile access
snow-code web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
snow-code attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
snow-code agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
snow-code agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
snow-code auth [command]
```

---

#### login

Snow-Flow is powered by the provider list at [Models.dev](https://models.dev), so you can use `snow-code auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/snow-code/auth.json`.

```bash
snow-code auth login
```

When Snow-Flow starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
snow-code auth list
```

Or the short version.

```bash
snow-code auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
snow-code auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
snow-code github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
snow-code github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
snow-code github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
snow-code mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
snow-code mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
snow-code mcp list
```

Or use the short version.

```bash
snow-code mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
snow-code mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
snow-code mcp auth list
```

Or use the short version.

```bash
snow-code mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
snow-code mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
snow-code mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
snow-code models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
snow-code models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Snow-Flow.

```bash
snow-code models --refresh
```

---

### run

Run snow-code in non-interactive mode by passing a prompt directly.

```bash
snow-code run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "snow-code run"
snow-code run Explain the use of context in Go
```

You can also attach to a running `snow-code serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
snow-code serve

# In another terminal, run commands that attach to it
snow-code run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                        |
| ------------ | ----- | ------------------------------------------------------------------ |
| `--command`  |       | The command to run, use message for args                           |
| `--continue` | `-c`  | Continue the last session                                          |
| `--session`  | `-s`  | Session ID to continue                                             |
| `--share`    |       | Share the session                                                  |
| `--model`    | `-m`  | Model to use in the form of provider/model                         |
| `--agent`    |       | Agent to use                                                       |
| `--file`     | `-f`  | File(s) to attach to message                                       |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)              |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided) |
| `--attach`   |       | Attach to a running snow-code server (e.g., http://localhost:4096)  |
| `--port`     |       | Port for the local server (defaults to random port)                |

---

### serve

Start a headless Snow-Flow server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
snow-code serve
```

This starts an HTTP server that provides API access to snow-code functionality without the TUI interface. Set `SNOW_CODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `snow-code`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage Snow-Flow sessions.

```bash
snow-code session [command]
```

---

#### list

List all Snow-Flow sessions.

```bash
snow-code session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your Snow-Flow sessions.

```bash
snow-code stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
snow-code export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or Snow-Flow share URL.

```bash
snow-code import <file>
```

You can import from a local file or an Snow-Flow share URL.

```bash
snow-code import session.json
snow-code import https://opncd.ai/s/abc123
```

---

### web

Start a headless Snow-Flow server with a web interface.

```bash
snow-code web
```

This starts an HTTP server and opens a web browser to access Snow-Flow through a web interface. Set `SNOW_CODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `snow-code`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
snow-code acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall Snow-Flow and remove all related files.

```bash
snow-code uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates snow-code to the latest version or a specific version.

```bash
snow-code upgrade [target]
```

To upgrade to the latest version.

```bash
snow-code upgrade
```

To upgrade to a specific version.

```bash
snow-code upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The snow-code CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

Snow-Flow can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `SNOW_CODE_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `SNOW_CODE_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `SNOW_CODE_CONFIG`                     | string  | Path to config file                               |
| `SNOW_CODE_CONFIG_DIR`                 | string  | Path to config directory                          |
| `SNOW_CODE_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `SNOW_CODE_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `SNOW_CODE_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `SNOW_CODE_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `SNOW_CODE_PERMISSION`                 | string  | Inlined json permissions config                   |
| `SNOW_CODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `SNOW_CODE_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `SNOW_CODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `SNOW_CODE_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `SNOW_CODE_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `SNOW_CODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `SNOW_CODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `SNOW_CODE_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `SNOW_CODE_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `SNOW_CODE_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `SNOW_CODE_SERVER_USERNAME`            | string  | Override basic auth username (default `snow-code`) |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `SNOW_CODE_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `SNOW_CODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `SNOW_CODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `SNOW_CODE_EXPERIMENTAL_BASH_MAX_OUTPUT_LENGTH`  | number  | Max output length for bash commands     |
| `SNOW_CODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `SNOW_CODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `SNOW_CODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `SNOW_CODE_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `SNOW_CODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
