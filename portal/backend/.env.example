# Snow-Flow Enterprise Portal - Environment Variables

# Server Configuration
PORT=8080
NODE_ENV=production
LOG_LEVEL=info

# Database Configuration (Cloud SQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=snow-flow
DB_PASSWORD=your-password-here
DB_NAME=licenses

# Cloud SQL Configuration (for production)
INSTANCE_CONNECTION_NAME=your-project:region:instance-name
INSTANCE_UNIX_SOCKET=/cloudsql/your-project:region:instance-name

# Security
JWT_SECRET=your-jwt-secret-key-change-in-production-256-bits-recommended
ADMIN_KEY=your-admin-api-key-change-in-production

# Credentials Encryption
# Option 1: Google Cloud KMS (RECOMMENDED for production)
GCP_PROJECT_ID=your-project-id
KMS_LOCATION=europe-west4
KMS_KEY_RING=credentials-keyring
KMS_KEY_NAME=credentials-key

# Option 2: Local AES-256-GCM (fallback, development only)
# MUST be exactly 32 bytes for AES-256
CREDENTIALS_ENCRYPTION_KEY=change-this-to-32-byte-secret!

# CORS Configuration
CORS_ORIGIN=*

# MCP Configuration (optional)
ENTERPRISE_MCP_URL=https://enterprise.snow-flow.dev
