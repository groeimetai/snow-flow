# Reporting

Reports, dashboards, and KPIs

**Total Tools:** 10 | **Read:** 3 | **Write:** 7

---

## snow_add_dashboard_widget

Add widget to dashboard

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_add_dashboard_widget
const result = await snow_add_dashboard_widget({
});
```

---

## snow_aggregate_metrics

Aggregate table metrics

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_aggregate_metrics
const result = await snow_aggregate_metrics({
});
```

---

## snow_create_dashboard

Create dashboard

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_dashboard
const result = await snow_create_dashboard({
});
```

---

## snow_create_dashboard

Create interactive dashboards with multiple widgets

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_dashboard
const result = await snow_create_dashboard({
});
```

---

## snow_create_metric

Create system metric

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_metric
const result = await snow_create_metric({
});
```

---

## snow_create_report

Create ServiceNow report

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_report
const result = await snow_create_report({
});
```

---

## snow_define_kpi

Define Key Performance Indicators for monitoring

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_define_kpi
const result = await snow_define_kpi({
});
```

---

## snow_get_mobile_analytics

Retrieves mobile application usage analytics including active users, sessions, and feature usage.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_get_mobile_analytics
const result = await snow_get_mobile_analytics({
});
```

---

## snow_pattern_analysis

Analyzes patterns across incidents, requests, and problems to identify trends, common issues, and improvement opportunities

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_pattern_analysis
const result = await snow_pattern_analysis({
});
```

---

## snow_schedule_report_delivery

Schedule automated report delivery via email

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_schedule_report_delivery
const result = await snow_schedule_report_delivery({
});
```

---

