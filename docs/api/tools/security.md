# Security

Security policies and compliance

**Total Tools:** 18 | **Read:** 9 | **Write:** 9

---

## snow_analyze_threat_intelligence

Analyzethreatintelligence

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_analyze_threat_intelligence
const result = await snow_analyze_threat_intelligence({
});
```

---

## snow_audit_trail_analysis

Audittrailanalysis

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_audit_trail_analysis
const result = await snow_audit_trail_analysis({
});
```

---

## snow_automate_threat_response

Automatethreatresponse

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_automate_threat_response
const result = await snow_automate_threat_response({
});
```

---

## snow_create_access_control

Createaccesscontrol

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_access_control
const result = await snow_create_access_control({
});
```

---

## snow_create_audit_rule

Createauditrule

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_audit_rule
const result = await snow_create_audit_rule({
});
```

---

## snow_create_compliance_rule

Creates compliance rules for regulatory frameworks (SOX, GDPR, HIPAA). Defines validation, remediation, and severity levels.

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_compliance_rule
const result = await snow_create_compliance_rule({
});
```

---

## snow_create_security_incident

Create security incident with automated threat correlation and priority assignment

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_security_incident
const result = await snow_create_security_incident({
});
```

---

## snow_create_security_policy

Creates security policies for access control and data protection. Configures enforcement levels, scope, and rule sets.

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_security_policy
const result = await snow_create_security_policy({
});
```

---

## snow_create_vulnerability_scan

Creates vulnerability scanning configurations. Schedules scans, sets severity thresholds, and enables auto-remediation.

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_vulnerability_scan
const result = await snow_create_vulnerability_scan({
});
```

---

## snow_discover_security_frameworks

Discovers security and compliance frameworks available in the instance for policy creation and auditing.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_discover_security_frameworks
const result = await snow_discover_security_frameworks({
});
```

---

## snow_discover_security_policies

Lists existing security policies and rules with filtering by category and active status.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_discover_security_policies
const result = await snow_discover_security_policies({
});
```

---

## snow_escalate_permissions

Escalate user permissions with approval workflow and time-based access controls

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_escalate_permissions
const result = await snow_escalate_permissions({
});
```

---

## snow_execute_security_playbook

Execute automated security response playbook with orchestrated actions

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_execute_security_playbook
const result = await snow_execute_security_playbook({
});
```

---

## snow_run_compliance_scan

Run compliance scans against security frameworks (SOX, GDPR, HIPAA, PCI-DSS)

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_run_compliance_scan
const result = await snow_run_compliance_scan({
});
```

---

## snow_scan_vulnerabilities

Scan for security vulnerabilities

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_scan_vulnerabilities
const result = await snow_scan_vulnerabilities({
});
```

---

## snow_security_dashboard

Generate real-time security operations dashboard with key metrics

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_security_dashboard
const result = await snow_security_dashboard({
});
```

---

## snow_security_risk_assessment

Perform comprehensive security risk assessment analyzing access controls, vulnerabilities, and security posture

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_security_risk_assessment
const result = await snow_security_risk_assessment({
});
```

---

## snow_vulnerability_risk_assessment

Assess vulnerability risk with automated CVSS scoring and remediation planning

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_vulnerability_risk_assessment
const result = await snow_vulnerability_risk_assessment({
});
```

---

