# Advanced AI/ML

AI classification, anomaly detection, and ML predictions

**Total Tools:** 52 | **Read:** 44 | **Write:** 8

---

## snow_activate_plugin

Activate ServiceNow plugin

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_activate_plugin
const result = await snow_activate_plugin({
});
```

---

## snow_ai_classify

Classify text using AI

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_ai_classify
const result = await snow_ai_classify({
});
```

---

## snow_analyze_data_quality

Analyze data quality including completeness, consistency, and accuracy metrics

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_analyze_data_quality
const result = await snow_analyze_data_quality({
});
```

---

## snow_anomaly_detection

Detect anomalies in data

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_anomaly_detection
const result = await snow_anomaly_detection({
});
```

---

## snow_autocomplete

Get autocomplete suggestions

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_autocomplete
const result = await snow_autocomplete({
});
```

---

## snow_backup_instance

Create instance backup

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_backup_instance
const result = await snow_backup_instance({
});
```

---

## snow_cache_get

Get value from cache

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_cache_get
const result = await snow_cache_get({
});
```

---

## snow_cache_set

Set value in cache

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_cache_set
const result = await snow_cache_set({
});
```

---

## snow_check_health

Check ServiceNow instance health

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_check_health
const result = await snow_check_health({
});
```

---

## snow_clone_instance

Clone ServiceNow instance

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_clone_instance
const result = await snow_clone_instance({
});
```

---

## snow_convert_es6_to_es5

Convert ES6+ JavaScript to ES5 for ServiceNow compatibility. Use this when debugging SyntaxErrors.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_convert_es6_to_es5
const result = await snow_convert_es6_to_es5({
});
```

---

## snow_create_devops_change

Create automated DevOps change request for deployments

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | high |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_devops_change
const result = await snow_create_devops_change({
});
```

---

## snow_create_devops_pipeline

Create DevOps pipeline for CI/CD automation

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_create_devops_pipeline
const result = await snow_create_devops_pipeline({
});
```

---

## snow_custom_plugin

Execute custom plugin logic

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_custom_plugin
const result = await snow_custom_plugin({
});
```

---

## snow_data_convert

Unified data conversion (csv_to_json, json_to_csv, json_to_xml, xml_to_json)

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_data_convert
const result = await snow_data_convert({
});
```

---

## snow_data_export

Export table data to CSV/XML/JSON format. Always includes sys_id. Returns records array with full data.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_data_export
const result = await snow_data_export({
});
```

---

## snow_data_mapper

Advanced data mapping with transformations

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_data_mapper
const result = await snow_data_mapper({
});
```

---

## snow_date_filter

Build date-based query filter

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_date_filter
const result = await snow_date_filter({
});
```

---

## snow_decode_base64

Decode Base64 to string

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_decode_base64
const result = await snow_decode_base64({
});
```

---

## snow_decode_url

URL decode string

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_decode_url
const result = await snow_decode_url({
});
```

---

## snow_diff_objects

Compare two objects and get differences

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_diff_objects
const result = await snow_diff_objects({
});
```

---

## snow_duplicate_detection

Detect duplicate records

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_duplicate_detection
const result = await snow_duplicate_detection({
});
```

---

## snow_encode_base64

Encode string to Base64

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_encode_base64
const result = await snow_encode_base64({
});
```

---

## snow_encode_url

URL encode string

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_encode_url
const result = await snow_encode_url({
});
```

---

## snow_field_filter

Create field-based filter

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_field_filter
const result = await snow_field_filter({
});
```

---

## snow_field_mapper

Map fields between schemas

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_field_mapper
const result = await snow_field_mapper({
});
```

---

## snow_format_date

Format date for ServiceNow

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_format_date
const result = await snow_format_date({
});
```

---

## snow_format_number

Format number with locale

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_format_number
const result = await snow_format_number({
});
```

---

## snow_format_text

Format text with various transformations

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_format_text
const result = await snow_format_text({
});
```

---

## snow_fuzzy_search

Perform fuzzy search across tables with relevance scoring. Returns sys_id + name + relevance_score for each match. Uses LIKE/CONTAINS queries and ranks by similarity.

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_fuzzy_search
const result = await snow_fuzzy_search({
});
```

---

## snow_generate_guid

Generate GUID/UUID

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_generate_guid
const result = await snow_generate_guid({
});
```

---

## snow_generate_records

Generate multiple test records

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_generate_records
const result = await snow_generate_records({
});
```

---

## snow_get_devops_insights

Retrieve DevOps metrics and insights

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_get_devops_insights
const result = await snow_get_devops_insights({
});
```

---

## snow_hash_string

Hash string using various algorithms

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_hash_string
const result = await snow_hash_string({
});
```

---

## snow_jwt_decode

Decode JWT token

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_jwt_decode
const result = await snow_jwt_decode({
});
```

---

## snow_list_plugins

List installed plugins

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_list_plugins
const result = await snow_list_plugins({
});
```

---

## snow_merge_objects

Deep merge multiple objects

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_merge_objects
const result = await snow_merge_objects({
});
```

---

## snow_ml_predict

Make ML prediction using trained model

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_ml_predict
const result = await snow_ml_predict({
});
```

---

## snow_paginate

Calculate pagination parameters

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_paginate
const result = await snow_paginate({
});
```

---

## snow_parse_json

Parse and validate JSON

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_parse_json
const result = await snow_parse_json({
});
```

---

## snow_query_filter

Build ServiceNow encoded query filter

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_query_filter
const result = await snow_query_filter({
});
```

---

## snow_random_string

Generate random string

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_random_string
const result = await snow_random_string({
});
```

---

## snow_rate_limit

Apply rate limiting to operations

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_rate_limit
const result = await snow_rate_limit({
});
```

---

## snow_recommendation_engine

Generate recommendations based on user behavior

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_recommendation_engine
const result = await snow_recommendation_engine({
});
```

---

## snow_retry_operation

Retry failed operation with backoff

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | intermediate |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_retry_operation
const result = await snow_retry_operation({
});
```

---

## snow_sanitize_input

Sanitize input for security

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_sanitize_input
const result = await snow_sanitize_input({
});
```

---

## snow_sentiment_analysis

Analyze text sentiment

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | advanced |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_sentiment_analysis
const result = await snow_sentiment_analysis({
});
```

---

## snow_sleep

Sleep for specified milliseconds

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | low |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_sleep
const result = await snow_sleep({
});
```

---

## snow_timestamp

Get current timestamp in various formats

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_timestamp
const result = await snow_timestamp({
});
```

---

## snow_track_deployment

Track application deployment through DevOps pipeline

| Property | Value |
|----------|-------|
| Permission | `write` |
| Complexity | beginner |
| Frequency | high |
| Allowed Roles | developer, admin |

### Example

```javascript
// Using snow_track_deployment
const result = await snow_track_deployment({
});
```

---

## snow_transform_data

Transform data using field mappings

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_transform_data
const result = await snow_transform_data({
});
```

---

## snow_velocity_tracking

Track team velocity and delivery metrics

| Property | Value |
|----------|-------|
| Permission | `read` |
| Complexity | beginner |
| Frequency | medium |
| Allowed Roles | developer, stakeholder, admin |

### Example

```javascript
// Using snow_velocity_tracking
const result = await snow_velocity_tracking({
});
```

---

